{
  "name": "template",
  "version": "1.0.0",
  "license": "Proprietary",
  "workspaces": [
    "workspaces/*"
  ],
  "scripts": {
    "watch": "nodemon --ignore '*/dist/*' --ext ts,js,mjs,json --exec yarn workspaces foreach run compile"
  },
  "packageManager": "yarn@3.5.1",
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
